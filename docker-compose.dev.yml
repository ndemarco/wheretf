services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: wheretf
      POSTGRES_USER: wheretf
      POSTGRES_PASSWORD: wheretf
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
